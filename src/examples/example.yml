description: >
  Sample example of semver tag update, new semver value will be in $VERSION.

usage:
  version: 2.1
  orbs:
    semver: tmo-orb/semver@latest
  workflows:
    use-my-orb:
      jobs:
        - semver/semver_increment
